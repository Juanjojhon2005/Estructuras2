import React from 'react';

const FirstApp = () => {
    const title = 'First App';
    const number = 10;

    return (
        <>
            <h1>{title}</h1>
            <span>{number}</span>
        </>
    );
};

export default FirstApp;
